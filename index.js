const{promises:fs}=require("fs"),path=require("path"),{headings:headings}=require("./heading");async function main(){const e=(await fs.readFile(path.join(process.cwd(),"./README.md.tmpl"))).toString("utf-8"),a=headings[Math.floor(Math.random()*headings.length)],t=new Date,i=new Intl.DateTimeFormat("en-US",{day:"numeric",month:"short",year:"numeric",hour:"numeric",minute:"2-digit",hour12:!0,timeZoneName:"short"}).format(t);let n=e.replace("{heading}",a);n=n.replace("{datetime}",i),await fs.writeFile("README.md",n)}main();